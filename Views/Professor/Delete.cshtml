@model Curso_Idiomas.Models.Professor

@{
    ViewData["Title"] = "Excluir professor";
}

<h1>Excluir</h1>
<h3>Tem certeza que deseja excluir esse professor?</h3>
<div>
    <hr />
    <h4>Professor</h4>

    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.ProfessorId)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.ProfessorId)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.NomeCompleto)
        </dt>
        <dd class="col-sm-10 h4 text-danger">
            @Html.DisplayFor(model => model.NomeCompleto)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Email)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Email)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.DataContratacao)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.DataContratacao)
        </dd>
        <dt class="col-sm-2">
            Turmas
        </dt>
        <dd>
            <table class="table table-secondary table-hover">
                <thead class="table-active">
                <tr>
                    <th>Disciplina</th>
                    <th>Horário</th>
                    <th>Semestre</th>
                    <th></th>
                </tr>
                </thead>
                @foreach (var turma in Model.Turmas)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => turma.Disciplina.Nome)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => turma.Horario)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => turma.Semestre)
                        </td>
                        <td>
                            <a asp-controller="Turma" asp-action="Details" asp-route-id="@turma.TurmaId" class="btn btn-sm btn-outline-info mx-1">Ver turma</a>
                        </td>
                    </tr>
                }
            </table>
        </dd>
    </dl>

    <form asp-action="Delete">
        <input type="hidden" asp-for="ProfessorId" />
        <input type="submit" value="Excluir professor" class="btn btn-danger" />
        <a asp-action="Index" class="btn btn-primary mx-4">Voltar</a>
    </form>
</div>

@model Curso_Idiomas.Models.ViewModels.ProfessorViewModel

@{
    ViewData["Title"] = "Professores";
}

<h1>@ViewData["Title"]</h1>

<p>
    <a asp-action="Create">Criar novo professor</a>
</p>

<form asp-action="Index" method="get">
    <div>
        <p>
            Filtrar por
            <select name="filtroEscolhido" asp-for="FiltroEscolhido" asp-items="Model.OpcoesFiltro"></select>:

            <input type="text" name="conteudoFiltro" value="@Model.ConteudoFiltro" />

            <input type="submit" value="Buscar" /> |

            <a asp-action="Index">Remover filtro</a>
        </p>
    </div>

</form>

<table class="table">
    <thead>
        <tr>
            <th>
                <a asp-action="Index"
                   asp-route-ordem="@Model.OrdemNome"
                   asp-route-filtroEscolhido="@Model.FiltroEscolhido"
                   asp-route-conteudoFiltro="@Model.ConteudoFiltro"> @Html.DisplayNameFor(model => model.Professores.First().Nome)</a>
            </th>
            <th>
                <a asp-action="Index"
                   asp-route-ordem="@Model.OrdemSobrenome"
                   asp-route-filtroEscolhido="@Model.FiltroEscolhido"
                   asp-route-conteudoFiltro="@Model.ConteudoFiltro"> @Html.DisplayNameFor(model => model.Professores.First().Sobrenome)</a>
            </th>
            <th>
                Turmas
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Professores)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Nome)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Sobrenome)
                </td>
                <td>
                    @item.Turmas.Count
                </td>
                <td>
                    <a asp-action="Details" asp-route-id="@item.ProfessorId">Detalhes</a> |
                    <a asp-action="Edit" asp-route-id="@item.ProfessorId">Edit</a> |
                    <a asp-action="Delete" asp-route-id="@item.ProfessorId">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
